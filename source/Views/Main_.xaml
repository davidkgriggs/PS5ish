<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                    mc:Ignorable="d">

    <Style TargetType="{x:Type Main}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Main}">
                    <Grid x:Name="PART_MainHost"
                          d:DesignWidth="{StaticResource DesignWidth}"
                          d:DesignHeight="{StaticResource DesignHeight}"
                          d:DataContext="{x:Static DesignMainViewModel.DesignIntance}">
                        <Grid x:Name="PART_ViewHost" Background="{DynamicResource MainBackgourndBrush}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <FadeImage x:Name="PART_ImageBackground" Grid.RowSpan="3"
                                       HorizontalAlignment="Center" VerticalAlignment="Top"/>

                            <!--Overlay Image-->
                            <StackPanel HorizontalAlignment="Stretch" Grid.Row="0" Grid.RowSpan="3" VerticalAlignment="Center" 
                                        Width="{StaticResource DesignWidth}" Height="{StaticResource DesignHeight}" 
                                        Background="Black" Opacity="0.3">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Settings Fullscreen.MainBackgroundImageDarkAmount}" Value="0.0">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                            </StackPanel>

                            <StackPanel Grid.RowSpan="3" HorizontalAlignment="Stretch"  Height="{StaticResource DesignHeight}">
                                <StackPanel.Style>
                                    <Style>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                <Setter Property="Control.Background" Value="{DynamicResource DarkLightBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                            </StackPanel>

                            <StackPanel Grid.RowSpan="3" Height="{StaticResource DesignHeight}">
                                <StackPanel.Style>
                                    <Style>
                                        <Setter Property="Control.Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                <Setter Property="Control.Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <Line Stroke="#909090" Opacity="0.5" StrokeThickness="2" Y1="{StaticResource DesignHeight}" HorizontalAlignment="Left" Grid.Row="0" Grid.RowSpan="3" Margin="482,0,0,0"/>
                            </StackPanel>

                            <Grid Grid.Row="0" Background="Transparent"
                                  Height="100">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="115" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="4*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="100" />
                                </Grid.ColumnDefinitions>

                                <!--Extra Metadata Tools Profile Picture-->
                                <DockPanel Grid.Column="1">
                                    <TextBlock Name="ProfilePicturePath" Tag="{DynamicResource ExtraMetadataPath}" Visibility="Collapsed">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text">
                                                    <Setter.Value>
                                                        <MultiBinding StringFormat="{}{0}{1}">
                                                            <Binding RelativeSource="{RelativeSource Self}" Path="Tag" />
                                                            <Binding Source="\ExtraMetadata\Themes\Common\ProfilePicture.png"/>
                                                        </MultiBinding>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <StackPanel Orientation="Horizontal" Grid.Column="1"  Tag="{DynamicResource UseAbsoluteExtraMetadataPath}">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="true">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <Border CornerRadius="60" Height="60" Width="60" VerticalAlignment="Bottom">
                                            <Border.Background>
                                                <ImageBrush RenderOptions.BitmapScalingMode="Fant" ImageSource="{Binding ElementName=ProfilePicturePath, Path=Text, Converter={StaticResource ImageStringToImageConverter}}"/>
                                            </Border.Background>
                                        </Border>
                                        <TextBlock Margin="15,43.718,0,13.718" Text="{DynamicResource ProfileName}" Style="{DynamicResource TextBlockBaseStyle}" Foreground="#FFEBEBEB" FontFamily="Segoe UI Semibold" FontSize="32" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" Grid.Column="1"  Tag="{DynamicResource UseAbsoluteExtraMetadataPath}">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="true">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <Border CornerRadius="60" Height="60" Width="60" VerticalAlignment="Bottom">
                                            <Border.Background>
                                                <ImageBrush RenderOptions.BitmapScalingMode="Fant" ImageSource="{ThemeFile 'Media/ProfilePicture.png'}"/>
                                            </Border.Background>
                                        </Border>
                                        <TextBlock Margin="15,43.718,0,13.718" Text="{DynamicResource ProfileName}" Style="{DynamicResource TextBlockBaseStyle}" Foreground="#FFEBEBEB" FontFamily="Segoe UI Semibold" FontSize="32" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DockPanel>

                                <ContentControl x:Name="PART_ElemBatteryStatus" Focusable="False" FontSize="42"
                                            VerticalAlignment="Bottom" Margin="10,0,10,0"
                                            Grid.Column="6">
                                    <ContentControl.Style>
                                        <Style TargetType="ContentControl">
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding PowerStatus.Charge}" Value="Critical">
                                                    <Setter Property="Content" Value="{DynamicResource BatteryStatusCritical}" />
                                                    <Setter Property="Foreground" Value="Red" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding PowerStatus.Charge}" Value="Low">
                                                    <Setter Property="Content" Value="{DynamicResource BatteryStatusLow}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding PowerStatus.Charge}" Value="Medium">
                                                    <Setter Property="Content" Value="{DynamicResource BatteryStatusMedium}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding PowerStatus.Charge}" Value="High">
                                                    <Setter Property="Content" Value="{DynamicResource BatteryStatusHigh}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding PowerStatus.IsCharging}" Value="True">
                                                    <Setter Property="Content" Value="{DynamicResource BatteryStatusCharging}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>

                                <TextBlock x:Name="PART_TextBatteryPercentage" Style="{DynamicResource TextBlockBaseStyle}"
                                       VerticalAlignment="Bottom" Margin="0,0,20,0"
                                       Grid.Column="7"/>

                                <TextBlock x:Name="PART_TextClock" Style="{DynamicResource TextBlockBaseStyle}" FontSize="26" Foreground="#FFEBEBEB"
                                       VerticalAlignment="Bottom" Margin="20,0,20,0"
                                       Grid.Column="10"/>

                                <ButtonEx x:Name="PART_ButtonNotifications" VerticalAlignment="Bottom"
                                          Margin="10,0,10,0" Height="48" Width="48"
                                          Grid.Column="8">
                                    <Button.Content>
                                        <Grid>
                                            <TextBlock Text="&#xeea3;" FontFamily="{StaticResource FontIcoFont}"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2"
                                                       Foreground="White" FontSize="30" />
                                            <TextBlock Text="&#x25CF;" Foreground="{DynamicResource GlyphBrush}"
                                                       HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                       FontSize="24" Margin="0,0,5,0" />
                                        </Grid>
                                    </Button.Content>
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource ButtonTopMenu}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Api Notifications.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Hidden"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </ButtonEx>

                                <ButtonEx x:Name="PART_ButtonMainMenu" VerticalAlignment="Bottom"
                                          Style="{DynamicResource ButtonTopMenu}"
                                          Margin="10,0,10,0" Height="48"
                                          Grid.Column="9">
                                    <Button.Content>
                                        <Image RenderOptions.BitmapScalingMode="Fant" Margin="2"
                                           Source="{ThemeFile 'Images/applogo_clean.png'}" />
                                    </Button.Content>
                                </ButtonEx>
                            </Grid>

                            <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="100" Grid.RowSpan="100" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="450" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="75" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="550"/>
                                    <RowDefinition Height="40"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <!--Show/Hide Grid Info-->
                                <Grid Grid.Row="0" Grid.Column="2" >
                                    <Grid.Style>
                                        <Style>
                                            <Setter Property="Control.Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                    <Setter Property="Control.Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,-35,0,0">
                                        <TextBlock Text="{DynamicResource LOCAllGames}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="44" ></TextBlock>
                                    </StackPanel>
                                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,80,160,0">
                                        <Border Background="{DynamicResource TabsBrush}" Width="400" Height="60" CornerRadius="5"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="30,90,160,0">
                                        <TextBlock Text="{DynamicResource LOCMenuSortByTitle}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="26" FontFamily="Segoe UI" >
                                        </TextBlock>
                                        <TextBlock Text=" " Style="{DynamicResource TextBlockBaseStyle}" FontSize="26" >
                                        </TextBlock>
                                        <StackPanel>
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PART_ToggleFilterRecently, Path=IsChecked}" Value="True">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="{DynamicResource LOCQuickFilterRecentlyPlayed}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="26" FontFamily="Segoe UI" />
                                        </StackPanel>
                                        <StackPanel>
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PART_ToggleFilterFavorite, Path=IsChecked}" Value="True">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="{DynamicResource LOCQuickFilterFavorites}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="26" FontFamily="Segoe UI" />
                                        </StackPanel>
                                        <StackPanel>
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PART_ToggleFilterMostPlayed, Path=IsChecked}" Value="True">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="{DynamicResource LOCQuickFilterMostPlayed}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="26" FontFamily="Segoe UI"/>
                                        </StackPanel>
                                        <StackPanel>
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PART_ToggleFilterAll, Path=IsChecked}" Value="True">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="{DynamicResource LOCQuickFilterAllGames}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="26" FontFamily="Segoe UI" />
                                        </StackPanel>
                                    </StackPanel>
                                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,80,50,0">
                                        <ButtonEx x:Name="PART_ButtonSearch" Height="60" Width="150" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                            <Button.Content>
                                                <TextBlock Text="&#59166;" FontFamily="Segoe MDL2 Assets" FontSize="32" Foreground="{DynamicResource TextBrush}"/>
                                            </Button.Content>
                                        </ButtonEx>
                                    </StackPanel>
                                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,80,210,0">
                                        <ButtonEx x:Name="PART_ButtonFilter" Height="60" Width="150" HorizontalContentAlignment="Center" VerticalContentAlignment="Center">
                                            <Button.Content>
                                                <TextBlock Text="&#59164;" FontFamily="Segoe MDL2 Assets" FontSize="32" Foreground="{DynamicResource TextBrush}"/>
                                            </Button.Content>
                                        </ButtonEx>
                                    </StackPanel>
                                </Grid>

                                <!--XBOX_ListGames-->
                                <StackPanel x:Name="HighlighterLogic">
                                    <StackPanel.Style>
                                        <Style>
                                            <Setter Property="Control.Visibility" Value="Hidden" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=PART_ListGameItems, Path=IsKeyboardFocusWithin}" Value="True">
                                                    <Setter Property="Control.Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                </StackPanel>

                                <Grid Margin="-214,0,0,0" Grid.Row="0" Grid.Column="2" VerticalAlignment="Bottom" HorizontalAlignment="Left" Focusable="False">
                                    <Grid.Style>
                                        <Style>
                                            <Setter Property="Control.Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                    <Setter Property="Control.Visibility" Value="Hidden" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <ListBoxEx x:Name="BehindGrid"
                                    Focusable="False"
                                    Background="Transparent" 
								    BorderThickness="0"
                                    VirtualizingPanel.IsContainerVirtualizable="True"     
                                    VirtualizingPanel.IsVirtualizing="True"
                                    VirtualizingPanel.ScrollUnit="Item" 
                                    VirtualizingPanel.VirtualizationMode="Recycling"     
                                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                    HorizontalContentAlignment="Left"     
                                    VerticalContentAlignment="Bottom"
                                    AlternationCount="{Binding ElementName=PART_ListGameItems, Path=Items.Count}"
                                    ItemContainerStyle="{DynamicResource RowOfGamesItems}"
                                               SelectedItem="{Binding ElementName=PART_ListGameItems, Path=SelectedItem}">
                                        <ListBoxEx.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <VirtualizingStackPanel Orientation="Horizontal" HorizontalAlignment="Left"></VirtualizingStackPanel>
                                            </ItemsPanelTemplate>
                                        </ListBoxEx.ItemsPanel>
                                        <ListBoxEx.Style>
                                            <Style>
                                                <Setter Property="ItemsControl.ItemsSource" Value="{Binding ElementName=PART_ListGameItems, Path=ItemsSource}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=PART_ElemGameStatus, Path=Visibility}" Value="Visible">
                                                        <Setter Property="ItemsControl.ItemsSource" Value="{Binding ElementName=BlankList, Path=ItemsSource}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ListBoxEx.Style>
                                    </ListBoxEx>
                                </Grid>

                                <Grid VerticalAlignment="Bottom" Grid.Row="0" Grid.Column="2">
                                    <StackPanel x:Name="SelectedIndex_1_Highlighter" VerticalAlignment="Bottom">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=SelectedIndexLogic, Path=Height}" Value="1">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Grid.Row="1" Focusable="False">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Hidden" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=HighlighterLogic, Path=Visibility}" Value="Visible">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <StackPanel Margin="2,0,0,-5" Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Left" Focusable="False">
                                                <Button Focusable="False" Tag="{DynamicResource Banners}" Style="{DynamicResource HighlighterSMALL1}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel x:Name="SelectedIndex_2_Highlighter" VerticalAlignment="Bottom">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=SelectedIndexLogic, Path=Height}" Value="2">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Grid.Row="1" Focusable="False">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Hidden" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=HighlighterLogic, Path=Visibility}" Value="Visible">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <StackPanel Margin="216,0,0,-5" Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Focusable="False">
                                                <Button Focusable="False" Tag="{DynamicResource Banners}" Style="{DynamicResource HighlighterSMALL2}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel x:Name="SelectedIndex_3_Highlighter" VerticalAlignment="Bottom">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=SelectedIndexLogic, Path=Height}" Value="3">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Grid.Row="1" Focusable="False">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Hidden" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=HighlighterLogic, Path=Visibility}" Value="Visible">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <StackPanel Margin="430,0,0,-5" Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Focusable="False">
                                                <Button Focusable="False" Tag="{DynamicResource Banners}" Style="{DynamicResource HighlighterSMALL3}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel x:Name="SelectedIndex_4_Highlighter" VerticalAlignment="Bottom">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=SelectedIndexLogic, Path=Height}" Value="4">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Grid.Row="1" Focusable="False">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Hidden" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=HighlighterLogic, Path=Visibility}" Value="Visible">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <StackPanel Margin="644,0,0,-5" Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Focusable="False">
                                                <Button Focusable="False" Tag="{DynamicResource Banners}" Style="{DynamicResource HighlighterSMALL4}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel x:Name="SelectedIndex_5_Highlighter" VerticalAlignment="Bottom">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=SelectedIndexLogic, Path=Height}" Value="5">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Grid.Row="1" Focusable="False">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Hidden" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=HighlighterLogic, Path=Visibility}" Value="Visible">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <StackPanel Margin="858,0,0,-5" Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Focusable="False">
                                                <Button Focusable="False" Tag="{DynamicResource Banners}" Style="{DynamicResource HighlighterSMALL5}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel x:Name="SelectedIndex_6_Highlighter" VerticalAlignment="Bottom">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=SelectedIndexLogic, Path=Height}" Value="6">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Grid.Row="1" Focusable="False">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Hidden" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=HighlighterLogic, Path=Visibility}" Value="Visible">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <StackPanel Margin="1072,0,0,-5" Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Focusable="False">
                                                <Button Focusable="False" Tag="{DynamicResource Banners}" Style="{DynamicResource HighlighterSMALL6}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>



                                <Grid Grid.Row="0" Grid.Column="1" Margin="0,0,0,0">
                                    <StackPanel Margin="0,0,0,0" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Left" Focusable="False">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                        <Setter Property="Control.Visibility" Value="Hidden" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <ListBoxEx x:Name="SelectedIndex0"
                                    Focusable="False"
                                    Background="Transparent" 
								    BorderThickness="0"
                                    VirtualizingPanel.IsContainerVirtualizable="True"     
                                    VirtualizingPanel.IsVirtualizing="True"
                                    VirtualizingPanel.ScrollUnit="Item" 
                                    VirtualizingPanel.VirtualizationMode="Recycling"     
                                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                    ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                    Height="480"
                                    Width="420"
                                    HorizontalContentAlignment="Left"
                                    VerticalContentAlignment="Bottom"
                                    AlternationCount="{Binding ElementName=PART_ListGameItems, Path=Items.Count}"
                                    ItemContainerStyle="{DynamicResource SelectedIndex0ListGameItem}"
                                                   SelectedItem="{Binding ElementName=PART_ListGameItems, Path=SelectedItem}">
                                            <ListBoxEx.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <VirtualizingStackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom" ></VirtualizingStackPanel>
                                                </ItemsPanelTemplate>
                                            </ListBoxEx.ItemsPanel>
                                            <ListBoxEx.Style>
                                                <Style>
                                                    <Setter Property="ItemsControl.ItemsSource" Value="{Binding ElementName=PART_ListGameItems, Path=ItemsSource}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ElementName=PART_ElemGameStatus, Path=Visibility}" Value="Visible">
                                                            <Setter Property="ItemsControl.ItemsSource" Value="{Binding ElementName=BlankList, Path=ItemsSource}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ListBoxEx.Style>
                                        </ListBoxEx>
                                    </StackPanel>
                                </Grid>

                                <Grid x:Name="SelectedIndex_0_Highlighter" Grid.Row="0" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Left">
                                    <Grid.Style>
                                        <Style>
                                            <Setter Property="Control.Visibility" Value="Hidden" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=SelectedIndexLogic, Path=Height}" Value="0">
                                                    <Setter Property="Control.Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <StackPanel Focusable="False">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Hidden" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=HighlighterLogic, Path=Visibility}" Value="Visible">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Margin="-5" Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Left" Focusable="False">
                                            <Button Focusable="False" Tag="{DynamicResource Banners}" Style="{DynamicResource HighlighterLARGE}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>

                                <DockPanel x:Name="ListItems" Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="4" VerticalAlignment="Top" Focusable="False">
                                    <ListBoxEx x:Name="PART_ListGameItems"
                                           FocusVisualStyle="{x:Null}"
										   VerticalAlignment="Top" 
										   HorizontalAlignment="Left"			
										   BorderThickness="0"
                                           VirtualizingPanel.IsContainerVirtualizable="True"     
                                           VirtualizingPanel.IsVirtualizing="True"
                                           VirtualizingPanel.ScrollUnit="Item"
                                           KeyboardNavigation.DirectionalNavigation="Once"
                                           ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                           AlternationCount="{Binding Items.Count, RelativeSource={RelativeSource Self}}">
                                        <ListBox.Style>
                                            <Style>
                                                <Setter Property="Control.Margin" Value="0,200,120,0" />
                                                <Setter Property="Control.Opacity" Value="0"/>
                                                <Setter Property="Control.Height" Value="50"/>
                                                <Setter Property="Control.Width" Value="1000000"/>
                                                <Setter Property="Control.Background" Value="Transparent" />
                                                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                        <Setter Property="Control.Opacity" Value="1"/>
                                                        <Setter Property="Control.Height" Value="860"/>
                                                        <Setter Property="Control.Margin" Value="490,150,0,0" />
                                                        <Setter Property="Control.Width" Value="1420"/>
                                                        <Setter Property="Control.Background" Value="Transparent"/>
                                                        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ListBox.Style>
                                    </ListBoxEx>
                                </DockPanel>

                                <StackPanel x:Name="SelectedIndexLogic"  Focusable="False">
                                    <StackPanel.Style>
                                        <Style>
                                            <Setter Property="Control.Height" Value="100"/>
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="0" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="0"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="1" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="1"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="2" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="2"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="3" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="3"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="4" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="4"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="5" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="5"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="6" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="6"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="7" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="7"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=SelectedIndex}" Value="8" />
                                                        <Condition Binding="{Binding ElementName=PART_ListGameItems, Path=Height}" Value="50" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Control.Height" Value="8"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                </StackPanel>


                                <!--Show/Hide Grid Info-->
                                <Grid x:Name="MoreGamesContainer" Visibility="Visible" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Focusable="False">
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Margin" Value="0,0,0,0"/>
                                                <Setter Property="Control.Focusable" Value="False"/>
                                                <Setter Property="Control.Visibility" Value="Visible"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                        <Setter Property="ItemsControl.Margin" Value="-90,-510,0,0"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <CheckBoxEx x:Name="MoreGames" Style="{DynamicResource MoreGamesStyle}"/>
                                    </StackPanel>
                                </Grid>



                                <StackPanel x:Name="InfoAndSuccessStory" Orientation="Horizontal" HorizontalAlignment="Left" Grid.Row="2" Grid.Column="2" VerticalAlignment="Top" Focusable="False" Margin="10,0,0,0">
                                    <StackPanel.Style>
                                        <Style>
                                            <Setter Property="Control.Margin" Value="0,0,0,0"/>
                                            <Setter Property="Control.Focusable" Value="False"/>
                                            <Setter Property="Control.Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                    <Setter Property="Control.Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>

                                    <Grid Tag="{DynamicResource Trailers}">
                                        <Grid.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="false">
                                                        <Setter Property="ItemsControl.Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Grid Tag="{DynamicResource UseAbsoluteExtraMetadataPath}">
                                            <Grid.Style>
                                                <Style>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="false">
                                                            <Setter Property="ItemsControl.Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <CheckBoxEx x:Name="Trailer" Margin="0,0,10,0">
                                                <ToggleButton.Style>
                                                    <Style TargetType="ToggleButton">
                                                        <Setter Property="Background" Value="Transparent" />
                                                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                                        <Setter Property="SnapsToDevicePixels" Value="True" />
                                                        <Setter Property="VerticalAlignment" Value="Center" />
                                                        <Setter Property="Focusable" Value="False"/>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Height" Value="215"/>
                                                        <Setter Property="MinHeight" Value="227"/>
                                                        <Setter Property="Width" Value="410"/>
                                                        <Setter Property="MinWidth" Value="418"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <Grid Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" Height="{TemplateBinding MinHeight}" Width="{TemplateBinding MinWidth}">
                                                                        <Border x:Name="mask" Visibility="Visible" Background="{DynamicResource OverlayMenuBackgroundBrush}" CornerRadius="5" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" HorizontalAlignment="Left" VerticalAlignment="Top"/>

                                                                        <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,0,12">
                                                                            <Grid.OpacityMask>
                                                                                <VisualBrush Visual="{Binding ElementName=mask}" RenderOptions.BitmapScalingMode="Fant"/>
                                                                            </Grid.OpacityMask>
                                                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                                <Grid Background="Black" Width="410" Height="215">
                                                                                    <TextBlock Name="VideoPath" Tag="{DynamicResource ExtraMetadataPath}" Visibility="Collapsed">
                                                                                        <TextBlock.Style>
                                                                                            <Style TargetType="TextBlock">
                                                                                                <Setter Property="Text">
                                                                                                    <Setter.Value>
                                                                                                        <MultiBinding StringFormat="{}{0}{1}{2}{3}">
                                                                                                            <Binding RelativeSource="{RelativeSource Self}" Path="Tag" />
                                                                                                            <Binding Source="\ExtraMetadata\games\"/>
                                                                                                            <Binding Path="SelectedGame.Id"/>
                                                                                                            <Binding Source="\VideoTrailer.mp4" />
                                                                                                        </MultiBinding>
                                                                                                    </Setter.Value>
                                                                                                </Setter>
                                                                                            </Style>
                                                                                        </TextBlock.Style>
                                                                                    </TextBlock>
                                                                                    <MediaElement MediaElementBehaviors.Repeat="True" Name="VideoSource" Tag="{DynamicResource UseAbsoluteExtraMetadataPath}" Stretch="Fill">
                                                                                        <MediaElement.Style>
                                                                                            <Style TargetType="MediaElement">
                                                                                                <Setter Property="Volume" Value="0"/>
                                                                                                <Setter Property="MediaElement.Source" Value="{Binding ElementName=VideoPath, Path=Text}"/>
                                                                                                <Style.Triggers>
                                                                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}, Path=IsActive}" Value="False">
                                                                                                        <Setter Property="MediaElement.Source" Value="" />
                                                                                                    </DataTrigger>
                                                                                                    <DataTrigger Binding="{Binding ElementName=Trailer,Path=Visibility}" Value="Collapsed">
                                                                                                        <Setter Property="MediaElement.Source" Value="" />
                                                                                                    </DataTrigger>
                                                                                                </Style.Triggers>
                                                                                            </Style>
                                                                                        </MediaElement.Style>
                                                                                    </MediaElement>
                                                                                </Grid>
                                                                            </StackPanel>
                                                                        </Grid>
                                                                    </Grid>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ToggleButton.Style>
                                            </CheckBoxEx>
                                        </Grid>
                                    </Grid>

                                    <StackPanel Orientation="Vertical"  Tag="{DynamicResource RightInfoTrueFalse}" Style="{DynamicResource RightInfo}">
                                        <StackPanel x:Name="Info" Tag="{PluginSettings Plugin=SuccessStory, Path=HasData}">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
                                                            <Setter Property="Control.Visibility" Value="Collapsed" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <CheckBoxEx x:Name="InfoCheckBox">
                                                <CheckBoxEx.Style>
                                                    <Style TargetType="CheckBoxEx">
                                                        <Setter Property="Background" Value="Transparent" />
                                                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                                        <Setter Property="SnapsToDevicePixels" Value="True" />
                                                        <Setter Property="VerticalAlignment" Value="Center" />
                                                        <Setter Property="Focusable" Value="False"/>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Height" Value="215"/>
                                                        <Setter Property="MinHeight" Value="227"/>
                                                        <Setter Property="Width" Value="410"/>
                                                        <Setter Property="MinWidth" Value="418"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <ControlTemplate.Resources>
                                                                        <Storyboard x:Key="IsClickedOpacity099">
                                                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(ToggleButton.Opacity)">
                                                                                <DiscreteDoubleKeyFrame  KeyTime="00:00:00.1" Value="0.99"/>
                                                                            </DoubleAnimationUsingKeyFrames>
                                                                        </Storyboard>
                                                                        <Storyboard x:Key="IsClickedOpacity1">
                                                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(ToggleButton.Opacity)">
                                                                                <DiscreteDoubleKeyFrame  KeyTime="00:00:00.1" Value="1"/>
                                                                            </DoubleAnimationUsingKeyFrames>
                                                                        </Storyboard>
                                                                    </ControlTemplate.Resources>
                                                                    <Grid Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" Height="{TemplateBinding MinHeight}" Width="{TemplateBinding MinWidth}">
                                                                        <Grid.RenderTransform>
                                                                            <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                                                                        </Grid.RenderTransform>
                                                                        <Border Visibility="Visible" Background="{DynamicResource OverlayMenuBackgroundBrush}" CornerRadius="5" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                                                                        <Border Visibility="Collapsed" Background="{DynamicResource UIBrush}" CornerRadius="5" Width="10" Height="{TemplateBinding Height}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,5,0"/>
                                                                        <TextBlock Margin="30,15,0,0" Text="{DynamicResource LOCSettingsGameDetailsLabel}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="40">
                                                                        </TextBlock>
                                                                        <StackPanel Orientation="Horizontal" >
                                                                            <StackPanel Margin="30,80,0,0">
                                                                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="28" Text="{DynamicResource LOCTimePlayed}" Margin="0,15,0,0" >
                                                                                </TextBlock>
                                                                                <StackPanel Margin="0,0,0,15" HorizontalAlignment="Left" Orientation="Horizontal" >
                                                                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="24" Text="{Binding SelectedGame.Playtime, Converter={StaticResource PlayTimeToStringConverter}}" >
                                                                                    </TextBlock>
                                                                                </StackPanel>
                                                                            </StackPanel>
                                                                        </StackPanel>
                                                                        <StackPanel Margin="0,95,30,0" HorizontalAlignment="Right" >
                                                                            <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="28" Text="{DynamicResource LOCPlatformTitle}" Margin="0,0,0,0" >
                                                                            </TextBlock>
                                                                            <StackPanel Margin="0,0,0,15" HorizontalAlignment="Right" Orientation="Horizontal" >
                                                                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="24" Text="{Binding SelectedGame.Platforms[0]}" TextWrapping="WrapWithOverflow" >
                                                                                </TextBlock>
                                                                            </StackPanel>
                                                                        </StackPanel>

                                                                        <Border Name="MainBorder" BorderThickness="4" BorderBrush="{DynamicResource UIBrush}" Height="{TemplateBinding MinHeight}" Width="{TemplateBinding MinWidth}" Margin="-6,-6,-6,6" Visibility="Collapsed" CornerRadius="5">
                                                                        </Border>
                                                                        <Border Name="BlurBorder" BorderThickness="4" BorderBrush="{DynamicResource UIBrush}" Height="{TemplateBinding MinHeight}" Width="{TemplateBinding MinWidth}" Margin="-6,-6,-6,6" Visibility="Collapsed" CornerRadius="5">
                                                                            <Border.Effect>
                                                                                <BlurEffect Radius="10"/>
                                                                            </Border.Effect>
                                                                        </Border>
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsFocused" Value="True">
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="BlurBorder" />
                                                                        </Trigger>
                                                                        <EventTrigger RoutedEvent="ToggleButton.Checked">
                                                                            <BeginStoryboard Storyboard="{StaticResource IsClickedOpacity099}"/>
                                                                        </EventTrigger>
                                                                        <EventTrigger RoutedEvent="ToggleButton.Unchecked">
                                                                            <BeginStoryboard Storyboard="{StaticResource IsClickedOpacity1}"/>
                                                                        </EventTrigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </CheckBoxEx.Style>
                                            </CheckBoxEx>
                                        </StackPanel>

                                        <StackPanel x:Name="SuccessStory" Tag="{PluginSettings Plugin=SuccessStory, Path=HasData}">
                                            <StackPanel.Style>
                                                <Style>
                                                    <Setter Property="Control.Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">
                                                            <Setter Property="Control.Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <CheckBoxEx x:Name="SuccessStoryCheckBox">
                                                <CheckBoxEx.Style>
                                                    <Style TargetType="CheckBoxEx">
                                                        <Setter Property="Background" Value="Transparent" />
                                                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                                        <Setter Property="SnapsToDevicePixels" Value="True" />
                                                        <Setter Property="VerticalAlignment" Value="Center" />
                                                        <Setter Property="Focusable" Value="False"/>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Setter Property="Height" Value="215"/>
                                                        <Setter Property="MinHeight" Value="227"/>
                                                        <Setter Property="Width" Value="410"/>
                                                        <Setter Property="MinWidth" Value="418"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ToggleButton">
                                                                    <ControlTemplate.Resources>
                                                                        <Storyboard x:Key="IsClickedOpacity099">
                                                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(ToggleButton.Opacity)">
                                                                                <DiscreteDoubleKeyFrame  KeyTime="00:00:00.1" Value="0.99"/>
                                                                            </DoubleAnimationUsingKeyFrames>
                                                                        </Storyboard>
                                                                        <Storyboard x:Key="IsClickedOpacity1">
                                                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(ToggleButton.Opacity)">
                                                                                <DiscreteDoubleKeyFrame  KeyTime="00:00:00.1" Value="1"/>
                                                                            </DoubleAnimationUsingKeyFrames>
                                                                        </Storyboard>
                                                                    </ControlTemplate.Resources>
                                                                    <Grid Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" Height="{TemplateBinding MinHeight}" Width="{TemplateBinding MinWidth}">
                                                                        <Grid.RenderTransform>
                                                                            <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1"/>
                                                                        </Grid.RenderTransform>
                                                                        <Border Visibility="Visible" Background="{DynamicResource OverlayMenuBackgroundBrush}" CornerRadius="5" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                                                                        <Border Visibility="Collapsed" Background="{DynamicResource UIBrush}" CornerRadius="5" Width="10" Height="{TemplateBinding Height}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,5,0"/>
                                                                        <TextBlock Margin="30,15,0,0" Text="{DynamicResource LOCSuccessStoryAchievements}" Style="{DynamicResource TextBlockBaseStyle}" FontSize="40">
                                                                        </TextBlock>
                                                                        <StackPanel Orientation="Horizontal" >
                                                                            <StackPanel Margin="30,70,0,0">
                                                                                <TextBlock Style="{DynamicResource ProgressLabel}" Margin="0,15,0,0" >
                                                                                </TextBlock>
                                                                                <StackPanel Margin="60,10,0,15" HorizontalAlignment="Left" Orientation="Horizontal" >
                                                                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="32" Text="{PluginSettings Plugin=SuccessStory, Path=Percent}" >
                                                                                    </TextBlock>

                                                                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="32" Text="%" >
                                                                                    </TextBlock>
                                                                                </StackPanel>
                                                                            </StackPanel>
                                                                        </StackPanel>
                                                                        <StackPanel Margin="0,85,30,0" HorizontalAlignment="Right" Orientation="Vertical" >
                                                                            <TextBlock Style="{DynamicResource EarnedLabel}" Margin="0,0,0,0" HorizontalAlignment="Right">
                                                                            </TextBlock>
                                                                            <StackPanel Margin="0,0,0,15" HorizontalAlignment="Right" Orientation="Horizontal" >
                                                                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="32" Text="{PluginSettings Plugin=SuccessStory, Path=Unlocked}" >
                                                                                </TextBlock>

                                                                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="32" Text="/" >
                                                                                </TextBlock>

                                                                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="32" Text="{PluginSettings Plugin=SuccessStory, Path=Total}" >
                                                                                </TextBlock>
                                                                            </StackPanel>
                                                                        </StackPanel>

                                                                        <Border Name="MainBorder" BorderThickness="4" BorderBrush="{DynamicResource UIBrush}" Height="{TemplateBinding MinHeight}" Width="{TemplateBinding MinWidth}" Margin="-6,-6,-6,6" Visibility="Collapsed" CornerRadius="5">
                                                                        </Border>
                                                                        <Border Name="BlurBorder" BorderThickness="4" BorderBrush="{DynamicResource UIBrush}" Height="{TemplateBinding MinHeight}" Width="{TemplateBinding MinWidth}" Margin="-6,-6,-6,6" Visibility="Collapsed" CornerRadius="5">
                                                                            <Border.Effect>
                                                                                <BlurEffect Radius="10"/>
                                                                            </Border.Effect>
                                                                        </Border>
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsFocused" Value="True">
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="MainBorder" />
                                                                            <Setter Property="Visibility" Value="Visible" TargetName="BlurBorder" />
                                                                        </Trigger>
                                                                        <EventTrigger RoutedEvent="ToggleButton.Checked">
                                                                            <BeginStoryboard Storyboard="{StaticResource IsClickedOpacity099}"/>
                                                                        </EventTrigger>
                                                                        <EventTrigger RoutedEvent="ToggleButton.Unchecked">
                                                                            <BeginStoryboard Storyboard="{StaticResource IsClickedOpacity1}"/>
                                                                        </EventTrigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </CheckBoxEx.Style>
                                            </CheckBoxEx>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>



                                <Grid Grid.Row="2" Grid.Column="1" >
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Margin" Value="0,250,0,10"/>
                                            <Setter Property="VerticalAlignment" Value="Bottom"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                    <Setter Property="Margin" Value="-90,-400,0,100"/>
                                                    <Setter Property="VerticalAlignment" Value="Top"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementName=PART_ElemFilters, Path=Visibility}" Value="Visible">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementName=PART_ElemFiltersAdditional, Path=Visibility}" Value="Visible">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ElementName=PART_ContentFilterItems, Path=Visibility}" Value="Visible">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    <FilterPresetSelector x:Name="PART_FilterPresetSelector" Margin="0,0,0,0" />
                                </Grid>

                                <!--XBOX_Warnings-->
                                <Grid Grid.Row="1">
                                    <StackPanel Margin="0,150,0,0" HorizontalAlignment="Center" 
                                        Grid.RowSpan="1" Grid.Row="1" VerticalAlignment="Center">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Settings Fullscreen.HorizontalLayout}" Value="True">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Orientation="Vertical" Width="1200">
                                            <Border Background="{DynamicResource OverlayMenuBackgroundBrush}" CornerRadius="15">
                                                <StackPanel>
                                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="72"
                                                       Margin="20" TextWrapping="WrapWithOverflow" TextAlignment="Center"
                                                       Text="For this Theme to work correctly DISABLE Horizontal Scrolling" >
                                                        <TextBlock.Effect>
                                                            <DropShadowEffect ShadowDepth="2" Color="Black" RenderingBias="Quality" Opacity="1"/>
                                                        </TextBlock.Effect>
                                                    </TextBlock>
                                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="72"
                                                       Margin="20" TextWrapping="WrapWithOverflow" TextAlignment="Center"
                                                       Text="Please go to SETTINGS to DISABLE Horizontal Scrolling" >
                                                        <TextBlock.Effect>
                                                            <DropShadowEffect ShadowDepth="2" Color="Black" RenderingBias="Quality" Opacity="1"/>
                                                        </TextBlock.Effect>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                                <!--XBOX_Warnings-->
                            </Grid>

                            <TextBlock Grid.Row="1" FontSize="40"
                                       Text="{DynamicResource LOCNoGamesFound}"
                                       TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockBaseStyle}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Items.Count, ElementName=PART_ListGameItems}" Value="0">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <Grid Grid.Row="2" Background="Transparent"
                              Height="100">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="550" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="950" />
                                    <ColumnDefinition Width="75" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right" Tag="{DynamicResource BottomTextTrueFalse}" Style="{DynamicResource BottomText}">
                                    <Border Background="#80000000" CornerRadius="5" Height="50"/>
                                    <StackPanel Orientation="Horizontal" >
                                        <!--Install-->
                                        <ButtonEx x:Name="PART_ButtonInstall"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCInstallGame}" />
                                        <!--Play-->
                                        <ButtonEx x:Name="PART_ButtonPlay"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCPlayGame}" />
                                        <!--Details-->
                                        <ButtonEx x:Name="PART_ButtonDetails"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCGameDetails}" />
                                        <!--Options-->
                                        <ButtonEx x:Name="PART_ButtonGameOptions"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCGameOptions}" />
                                        <!--Search-->
                                        <ButtonEx x:Name="PART_ButtonSearchh"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCSearchLabel}" />
                                        <!--Filter-->
                                        <ButtonEx x:Name="PART_ButtonFilterr"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCFilter}" />
                                    </StackPanel>
                                </Grid>
                                <StackPanel Grid.Column="2" x:Name="PART_ElemSearchActive" Margin="0,0,0,0"
                                            VerticalAlignment="Top" HorizontalAlignment="Stretch">
                                    <TextBlock  FontSize="18"                                                
                                                TextAlignment="Left"
                                                Style="{DynamicResource TextBlockBaseStyle}"
                                                Foreground="#FFEBEBEB"
                                                Text="Showing search results for:"/>
                                    <TextBlock  Foreground="#FFEBEBEB"
                                                TextAlignment="Left"
                                                Style="{DynamicResource TextBlockBaseStyle}"
                                                Text="{Binding AppSettings.Fullscreen.FilterSettings.Name}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2">
                                    <StackPanel.Style>
                                        <Style>
                                            <Setter Property="Control.Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ElementName=MoreGames, Path=IsChecked}" Value="True">
                                                    <Setter Property="Control.Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Grid.Column="2" x:Name="PART_ElemExtraFilterActive" Margin="0,55,0,0"
                                        FontSize="16" FontWeight="Light"
                                       VerticalAlignment="Center" HorizontalAlignment="Stretch"
                                       TextAlignment="Left"
                                       Style="{DynamicResource TextBlockBaseStyle}"
                                       Foreground="#FFEBEBEB"    
                                       Text="{DynamicResource LOCGameListFiltered}"/>
                                </StackPanel>

                                <StackPanel x:Name="PART_ElemProgressIndicator" Orientation="Horizontal" Margin="0,0,0,0"
                                        Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Left">
                                    <TextBlock x:Name="PART_TextProgressTooltip" 
                                           Margin="0,0,15,0"  FontSize="16" FontWeight="Light" VerticalAlignment="Center"
                                           Style="{DynamicResource TextBlockBaseStyle}" Foreground="#FFEBEBEB"/>
                                    <ContentControl Style="{DynamicResource ProgressIndicator}" Focusable="False"
                                                Height="30" Width="30" VerticalAlignment="Center" />
                                </StackPanel>

                                <!--XBOX_Warnings-->
                                <Grid Grid.Row="1">
                                    <StackPanel Margin="0,150,0,0" HorizontalAlignment="Center" 
                                        Grid.RowSpan="1" Grid.Row="1" VerticalAlignment="Center">
                                        <StackPanel.Style>
                                            <Style>
                                                <Setter Property="Control.Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Settings Fullscreen.HorizontalLayout}" Value="True">
                                                        <Setter Property="Control.Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <StackPanel Orientation="Vertical" Width="1200">
                                            <Border Background="{DynamicResource OverlayMenuBackgroundBrush}" CornerRadius="15">
                                                <StackPanel>
                                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="72"
                                                       Margin="20" TextWrapping="WrapWithOverflow" TextAlignment="Center"
                                                       Text="For this Theme to work correctly DISABLE Horizontal Scrolling" >
                                                        <TextBlock.Effect>
                                                            <DropShadowEffect ShadowDepth="2" Color="Black" RenderingBias="Quality" Opacity="1"/>
                                                        </TextBlock.Effect>
                                                    </TextBlock>
                                                    <TextBlock Style="{DynamicResource TextBlockBaseStyle}" FontSize="72"
                                                       Margin="20" TextWrapping="WrapWithOverflow" TextAlignment="Center"
                                                       Text="Please go to SETTINGS to DISABLE Horizontal Scrolling" >
                                                        <TextBlock.Effect>
                                                            <DropShadowEffect ShadowDepth="2" Color="Black" RenderingBias="Quality" Opacity="1"/>
                                                        </TextBlock.Effect>
                                                    </TextBlock>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                                <!--XBOX_Warnings-->
                            </Grid>
                            
                            <!--Filter Menu-->
                            <DockPanel x:Name="Filters" Grid.Row="1" Grid.RowSpan="2" Margin="0,145,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
                                <Filters x:Name="PART_ElemFilters"
                                         DockPanel.Dock="Left" Focusable="False" />

                                <FiltersAdditional x:Name="PART_ElemFiltersAdditional"
                                                   DockPanel.Dock="Left" Focusable="False" />

                                <ContentControl x:Name="PART_ContentFilterItems"
                                            DockPanel.Dock="Left" Focusable="False" />
                            </DockPanel>
                        </Grid>

                        <GameDetails x:Name="PART_ElemGameDetails" Grid.RowSpan="3"
                                     HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

                        <GameStatus x:Name="PART_ElemGameStatus" Grid.RowSpan="3"
                                     HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>